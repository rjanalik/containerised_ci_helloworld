build_job_test_image_libtree-{{os}}{{osver}}-spack{{spackver}}-{{archstr}}:
  stage: build_test_image
  extends:
    - {{container_builder}}
  variables:
    DOCKERFILE: "test/Dockerfile.libtree"
    PERSIST_IMAGE_NAME: "{{registry_path}}/test-libtree:spack{{spackver}}-{{os}}{{osver}}-{{archstr}}"
    DOCKER_BUILD_ARGS: '["BASEIMG={{registry_base_image_name_tag}}", "HELPERIMG={{registry_helper_image_name_tag}}", "TARGET={{spack_target}}"]'

test_job_base_image_libtree-{{os}}{{osver}}-spack{{spackver}}-{{archstr}}:
  stage: test_test_image
  extends:
    - {{container_runner}}
  image: "{{registry_path}}/test-libtree:spack{{spackver}}-{{os}}{{osver}}-{{archstr}}"
  script:
    - which libtree
